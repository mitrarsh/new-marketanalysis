import Body from "../body/Body";
import Header from "./Header";

const HeaderContainer = () => {
  return (
    <div className=" header-container layout-section-header">
      <Header />
    </div>
  );
};

export default HeaderContainer;
